var n = 25
var first = 0
var second = 1
var next = 0
var c = 0

print("The first " + n + " fibonacci numbers are:")

while c < n {
	if c <= 1 {
		next = c
	} else {
		next = first + second
		first = second
		second = next
	}
	print(next)
	c += 1
}

/* This is a block comment */
